{
  "name": "ui",
  "version": "0.1.0",
  "private": true,
  "//dependencies": {
    "//dnd-kit": "used to drag and drop steps into the pipeline editor canvas.",
    "//@dnd-kit/sortable": "enables sortable IO in the pipeline editor's IO pane.",
    "//@emotion": "provides styles for Material UI components.",
    "//@monaco-editor/react": "provides the YAML/code editor in the YAML input and pipeline editor's metadata pane.",
    "//@mui": "provides Material components and icons used across UI controls.",
    "//@ngageoint/geopackage": "reads GeoPackage data sources.",
    "//@ngageoint/leaflet-geopackage": "renders GeoPackage layers on Leaflet maps.",
    "//@testing-library/jest-dom": "Currently unused. Adds DOM assertions for component tests.",
    "//@testing-library/react": "Currently unused. Renders and exercises React components in tests.",
    "//@turf/turf": "performs geospatial calculations and geometry transforms.",
    "//axios": "axios is used for HTTP API requests to backend services.",
    "//elkjs": "is used to automatically layout nodes in the pipeline editor using the 'Layout' button",
    "//js-yaml": "parse and serialize YAML metadata for pipelines and scripts",
    "//leaflet": "provides the base interactive mapping engine in the run ui map previews.",
    "//react-leaflet": "integrates Leaflet maps into React components.",
    "//lodash": "lodash provides utility helpers used throughout UI logic.",
    "//ol": "OpenLayers map in location chooser, to handle CRS visualisation.",
    "//react-flow-renderer": "renders and manages the pipeline editor graph canvas.",
    "//react-markdown": "renders markdown content, especially in step metadata.",
    "//react-router-dom": "client-side routing between UI pages.",
    "//react-select": "Searchable select input to choose the script/pipeline to run.",
    "//web-vitals": "Currently unused: frontend performance metrics. Was there in project template."
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/modifiers": "^9.0.0",
    "@dnd-kit/sortable": "^10.0.0",
    "@emotion/react": "^11.13.3",
    "@emotion/styled": "^11.13.0",
    "@fontsource/roboto": "^5.1.0",
    "@monaco-editor/react": "^4.7.0",
    "@mui/icons-material": "^7.3.9",
    "@mui/material": "^7.3.9",
    "@ngageoint/geopackage": "^4.2.6",
    "@ngageoint/leaflet-geopackage": "^4.1.3",
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^11.2.7",
    "@turf/turf": "^7.2.0",
    "axios": "^1.9.0",
    "bon_in_a_box_script_service": "file:BonInABoxScriptService",
    "elkjs": "^0.8.2",
    "js-yaml": "^4.1.0",
    "leaflet": "^1.9.4",
    "react-leaflet": "^4.2.1",
    "lodash": "^4.17.21",
    "ol": "^10.5.0",
    "ol-mapbox-style": "^13.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-flow-renderer": "^10.3.17",
    "react-markdown": "^9.0.1",
    "react-router-dom": "^6.14.2",
    "react-select": "^5.8.0",
    "web-vitals": "^1.1.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.0",
    "@watergis/maplibre-gl-terradraw": "^1.3.14",
    "vite": "^6.3.0"
  },
  "scripts": {
    "start": "vite --host --no-open",
    "build": "vite build",
    "preview": "vite preview",
    "test": "react-scripts test"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "overrides": {
    "proj4": "^2.17.0"
  }
}