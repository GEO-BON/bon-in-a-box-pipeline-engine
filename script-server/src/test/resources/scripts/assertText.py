import sys, json;
print("start")
# Reading input.json
data = biab_inputs()
print(data)
stringIn = data['input']

# Do stuff.
if stringIn is None:
    biab_error_stop("This is null")

if type(test_string) != str
     biab_error_stop("Not a string")

# Serializing output.json
biab_output("the_same", stringIn)
